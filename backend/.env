# backend/.env
PORT=7542
NODE_ENV=development

# must point to the FRONTEND, not the backend
CLIENT_URL=http://localhost:3000

JWT_SECRET=super-secret-change-me-please

# match what my code uses (db.js currently points to ../data/db.db)
SQLITE_DB=./data/db.db
